# GH-20 Release Gate — Revision 1

`production_transport`: BLOCKED.

The private Firestore-polling design repairs the insecure public-ingress architecture and passes repository verification. The gate cannot PASS until a server-side Google Firestore credential is assigned in n8n, a synthetic pending lead is processed on the real project, live Firestore rules are verified/deployed, and the public AI Studio form is synchronized with the pending-status contract.

No security requirement is waived. Quick Tunnel is shut down and is not part of production.
